---
title: "Plotly Charts Display in Google Colab"
author: 'Pinn Xu'
date: 2025-10-07
order: -20251007   # sidebar sort key: negative date => newest first
description: 'How Plotly render inside Colab and in the browser after exporting notebook to HTML.'
categories: [Python, Colab, Plotly, Tutorial]
image: "https://raw.githubusercontent.com/Pinn32/img/main/img/pic-go/20260712154436828.png"

aliases:
    - ../colab-plotly-export/
---

# Summary
## Problem
After editing an `.ipynb` file in Google Colab and exporting it to HTML, the Plotly charts fail to display.

## Solution
After testing, in Google Colab the `.ipynb` file behaves as follows [^1]:

- `fig.show()` displays the chart correctly.
- `fig.show(renderer="notebook_connected")` fails to display the chart.

But once exported to HTML and opened in a browser [^2]:

- Charts generated by `fig.show()` fail to display.
- Only charts generated by `fig.show(renderer="notebook_connected")` display correctly.

So when editing a notebook in Colab, use the default renderer (`fig.show()`), and switch the `renderer` to `"notebook_connected"` right before exporting to HTML.


[^1]: **IPYNB file:** View the [demo file in Colab](https://colab.research.google.com/drive/1BbMO74Hw-yHjvXoj5mbD5DEZsw00RKnp?usp=drive_link) or <a href="../../../src/data/colab-plotly-export/renderer-test.ipynb" download>download it</a>

[^2]: **HTML file:** View the [demo file in the browser](../../../src/data/colab-plotly-export/renderer-test.html) or <a href="../../../src/data/colab-plotly-export/renderer-test.html" download>download it</a>


## Notes
After editing an `.ipynb` file in Colab, if you want to export it to HTML, be sure to press **Ctrl+S** first (saving to Google Drive) so that you export the most recently saved version. If you run the export command without saving first, the exported HTML may not be the latest version, and it may even have missing content or other issues.

**Recommended workflow:**

1. Finish editing the entire `ipynb`.
2. Choose **Runtime → Restart and run all**.
3. Press **Ctrl+S** to save the notebook.
4. Run the code below to export the HTML.
5. After the export finishes, if you made further changes to the notebook, press **Ctrl+S** once more to save.

```bash
%%shell
jupyter nbconvert --to html /content/drive/MyDrive/path/filename.ipynb
```

:::{.callout-tip}
`%%shell` is a cell magic of Google Colab ( `%%bash` in Jupyter ) used to run shell commands within a code cell. For a single-line shell command, you can also use `!`, such as `!pip install pyvis`.
:::
